import React from "react";

/** Used when a user navigates to a page that doesn't exist */
function Error404Page() {
	// @TODO: Make this into a real page
	return <p>Lol this page does not exist</p>;
}

export default Error404Page;
